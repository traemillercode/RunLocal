import { describe, expect, it } from "vitest";
import { Db } from "../src/server/store";
import { createSafetyReport } from "../src/server/safety";
const acct=(id:string,cityId:string,status:"verified"|"pending"="verified")=>({id,name:id,email:id+"@x.test",username:null,cityId,status,phase:"email" as const,role:"runner" as const,adminCityId:null,rolePriorAdmin:null,requestedRole:null,profilePhotoRef:null,supabaseAuthId:null,phone:null,phoneVerified:false,phoneVerifiedAt:null,birthdate:null,selfieRef:null,selfieCapturedAt:null,signupIp:null,signupAt:"2026-01-01",lastActivityAt:"2026-01-01",loginIps:[],verifiedAt:status==="verified"?"2026-01-01":null,deletedAt:null,purgeAt:null,purgedAt:null,retentionYears:3,suspended:false,suspendedUntil:null,suspensionReason:null,underReview:false,underReviewAt:null});
describe("safety reporting",()=>{it("gates context, privacy and duplicate/rate abuse",()=>{const db=new Db();const a=acct("a","c"),b=acct("b","c");db.createAccount(a); db.createAccount(b);db.addAttendance({id:"1",accountId:"a",eventId:"e",role:"rsvp",createdAt:"x"});db.addAttendance({id:"2",accountId:"b",eventId:"e",role:"rsvp",createdAt:"x"});const x=createSafetyReport(db,a,{subjectId:"b",cityId:"c",contextType:"event",contextId:"e",reason:"unsafe"});console.log(x); expect(x.ok).toBe(true);if(x.ok){const stored=db.getSafetyReport(x.data.id)!;expect(stored.reporterId).toBe("a");expect("reporterId" in ({id:stored.id,status:stored.status})).toBe(false);}expect(createSafetyReport(db,a,{subjectId:"b",cityId:"c",contextType:"event",contextId:"e",reason:"unsafe"})).toMatchObject({error:"duplicate_report"});});it("denies pending, deleted and cross-city",()=>{const db=new Db();const a=acct("a","c","pending"),b=acct("b","d");db.createAccount(a);db.createAccount(b);expect(createSafetyReport(db,a,{subjectId:"b",cityId:"c",contextType:"event",contextId:"e",reason:"unsafe"})).toMatchObject({status:403});});});
