import { useEffect, useState } from "react";
import * as api from "../lib/api";
export function GroupDetailPage({ id }: { id: string }) { const [group,setGroup]=useState<api.PublicUserGroup|null>(null); useEffect(()=>{void api.getPublicGroup(id).then(r=>{if(r.ok)setGroup(r.data.group)});},[id]); if(!group)return <section className="mx-auto max-w-3xl px-4 py-8"><p>Loading group…</p></section>; return <section className="mx-auto max-w-3xl px-4 py-6"><img src={group.coverPhotoUrl} alt="" className="h-48 w-full rounded-2xl object-cover"/><div className="mt-5 flex gap-4"><img src={group.logoPhotoUrl} alt="" className="h-20 w-20 rounded-2xl object-cover"/><div><h1 className="text-3xl font-black">{group.name}</h1><p className="text-slate-600">{group.groupType === "rrca-chartered" ? "RRCA-Chartered Club" : "Community Run Group"}{group.rrcaVerified ? " · RRCA verified" : ""}</p></div></div><p className="mt-5 whitespace-pre-wrap">{group.description}</p><h2 className="mt-6 font-bold">Group leaders</h2><ul className="mt-2">{group.leaders.map(l=><li key={l.id}>{l.name}</li>)}</ul><p className="mt-5 text-sm text-slate-600">Membership: {group.membershipMode === "open" ? "Open" : "Request to join"}</p><div className="mt-4 flex flex-wrap gap-3">{[group.websiteUrl,group.groupmeUrl,group.facebookUrl,group.instagramUrl].filter(Boolean).map((u,i)=><a className="text-orange-700 underline" href={u!} target="_blank" rel="noreferrer" key={u}>{["Website","GroupMe","Facebook","Instagram"][i]}</a>)}</div></section>; }
